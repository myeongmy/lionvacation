<header>
<div class="nav">
<!--<%= link_to 'home으로 가기', "/home/start"%>-->
<%= link_to '| 글쓰러가기 |', "/home/new_food"%>
<% if user_signed_in? %>
<%end%>
</div>
</header>

<br><br>

<body>
<section id="album_section">
<%@foods.each do |x|%>
    
<div class="album_box">
<%= link_to "#{x.title}", "/home/food_detail/#{x.id}"%>
| <%=x.email%> <br>
       
<div class="image_box">
<%= link_to image_tag(x.image.url, :class =>"style_image"), "/home/food_detail/#{x.id}"%><br>
</div>

<% if x.email == current_user.email %>
<%= link_to '[삭 제]', "/home/destroy/#{x.id}"%>
<%= link_to '[수 정]',"/home/edit/#{x.id}"%><br>
<%end%>
</div>

<%end%>
</section>
</body>