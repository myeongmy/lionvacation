<header>
<div class="nav">
<!--<%= link_to 'home으로 가기', "/home/start"%>-->
<%= link_to '| 글쓰러가기 |', "/home/new_location"%>
<% if user_signed_in? %>
<%end%>
</div>
</header>

<br><br>

<body>
<section id="album_section">
<%@locations.each do |x|%>

<div class="album_box">
<%= link_to "#{x.title}", "/home/location_detail/#{x.id}"%>
| <%=x.email%> <br>

<div class="image_box">
<%= link_to image_tag(x.image.url, :class =>"style_image"), "/home/location_detail/#{x.id}"%><br>
</div>

<% if x.email == current_user.email %>
<%= link_to '[삭 제]' ,"/home/destroy_location/#{x.id}"%><br>
<%= link_to '[수 정]', "/home/edit_location/#{x.id}"%><br>
<%end%>
</div>

<%end%>
</section>
</body>