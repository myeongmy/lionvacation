<header>
<div class="nav">
<!--<%= link_to 'home으로 가기', "/home/start"%>-->
<%= link_to '| 글쓰러가기 |', "/home/new_thing"%>
<% if user_signed_in? %>
<%end%>
</div>
</header>

<br><br>

<body>
<section id="album_section">
<%@things.each do |x|%>
<div class="album_box">
<%= link_to "#{x.title}", "/home/thing_detail/#{x.id}"%>
| <%=x.email%> <br>
<div class="image_box">
<%= link_to image_tag(x.image.url, :class =>"style_image"), "/home/thing_detail/#{x.id}"%><br>
</div>

<% if x.email == current_user.email %>
<%= link_to '[삭 제]',"/home/destroy_thing/#{x.id}"%><br>
<%= link_to '[수 정]',"/home/edit_thing/#{x.id}"%><br>
<%end%>
</div>

<%end%>
</section>
</body>